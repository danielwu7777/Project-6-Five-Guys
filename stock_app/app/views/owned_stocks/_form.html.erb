<%= form_with(model: owned_stock) do |form| %>
  <% if owned_stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(owned_stock.errors.count, "error") %> prohibited this owned_stock from being saved:</h2>

      <ul>
        <% owned_stock.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :ticker %>
    <%= form.text_field :ticker %>
  </div>

  <div class="field">
    <%= form.label :shares_owned %>
    <%= form.number_field :shares_owned %>
  </div>

  <div class="field">
    <%= form.label :total_cost %>
    <%= form.text_field :total_cost %>
  </div>

  <div class="field">
    <%= form.label :stock_id %>
    <%= form.number_field :stock_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
